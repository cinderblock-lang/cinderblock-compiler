namespace Ast.Entity {
  using Metadata;

  export struct Enum {
    name: string;
    properties: [Property];
    namespace: string;
    using: [string];

    exported: bool;
    c_name: string;
    location: CodeLocation;
  }
}